# 부표 (Buoy) — 프로젝트 컨텍스트

이 문서는 기획 대화의 결론만 압축한 것이다. **여기 적힌 결정은 이미 논의를 거친 것이니 다시 제안하지 말고 그대로 구현할 것.**

---

## 한 줄 정의

매일 일기를 쓴다. 일주일에 몇 번, 예고 없이, 그중 **문단 하나**가 익명으로 낯선 사람들에게 공개된다. 사람들은 편들어주는 버튼을 누른다.

---

## 확정된 제품 결정 — 뒤집지 말 것

| 결정 | 내용 | 이유 |
|---|---|---|
| **발췌 단위** | 일기 전체가 아니라 **문단 하나** | 맥락이 잘려야 노출 부담이 줄고, 동시에 글이 시처럼 읽힌다 |
| **공개 시점** | 사용자가 못 정한다. 시스템이 무작위로 | "언제 나갈지 모름"이 이 앱의 긴장 전부 |
| **공개 빈도** | 사용자가 정한다 (주 1/3/5/7회, 기본 3) | 강도는 내가 정하고 순간은 못 정한다 = "느슨하고 다정한 감시" |
| **노출 범위** | 사용자에게 권한 없음 | "누구한테 보여줄까"를 고르면 일기가 아니라 게시물이 된다 |
| **정체성** | 고정 닉네임. **프로필 페이지·팔로우·DM 전부 없음** | 닉네임이 계정이 되면 일기가 콘텐츠로 변질된다 |
| **AI 답장** | **없다.** 사용자가 명시적으로 거부함 | 독자는 사람이어야 한다. AI는 무대 뒤에서만 (필터·번역·팔레트) |
| **반응** | 힐링 아님. **편들기** — 줘패자 / 그냥 누워 / 잘했다 / 이 문장 좋다 | "괜찮아요"는 세 번째부터 공허하다 |
| **자유 댓글** | **없다.** 버튼만 | 익명에서 자유 텍스트를 열면 반드시 험해진다 |
| **반응 숫자** | 남의 발췌엔 안 보임. **내 발췌에만** 보임 | 인정욕구는 채우고 눈치싸움은 막는다 |
| **욕 처리** | 서랍엔 원문 그대로. 나갈 때만 귀여운 말로 **번역** | 배설을 막으면 앱을 안 쓴다. 화는 남기고 공격성만 뺀다 |
| **번역 흔적** | 치환된 단어에 **점선 밑줄** | 원래 뭐였는지 알아야 웃기고 화도 전달된다. 이게 시그니처 |
| **번역 미리보기** | **없다.** 공개된 뒤에만 볼 수 있다 | 웃기려고 일부러 욕 쓰는 걸 막는다 |
| **피드** | 하루 몫이 정해져 있고 다 읽으면 끝. **무한스크롤 없음** | 일기 앱이 SNS가 되는 걸 막는 장치 |
| **공개 시각** | 매일 밤 10시 동시 오픈 | 반응이 몰려야 반응 같다. 흩어지면 발췌당 2개 붙고 끝 |
| **최소 숙성** | 하룻밤 (작성 다음 날부터 후보) | 술 마시고 쓴 글이 그 밤에 나가는 사고를 막는다 |
| **후보 풀** | 계속 쌓인다. 3개월 전 문단도 오늘 뽑힐 수 있다 | 잊고 있던 문장이 발견되는 게 이 앱 최고의 순간 |

---

## 오늘 스코프

### 만들 것
1. 매직링크 로그인 + 닉네임 자동 배정
2. 일기 쓰기 → 빈 줄로 문단 분리
3. 문단별 자물쇠 — **기본값 잠김 (`locked default true`)**
4. 추첨 (랜덤 + 욕 사전 치환) — 수동 트리거 버튼
5. 표류 피드 + 반응 버튼 4개
6. 내 발췌 화면 (내 것에만 숫자)

### 안 만들 것 — 물어보지 말고 그냥 빼기
- **LLM 안전 필터** ← 오늘의 병목. `locked default true`로 대체
- 음성 입력 / STT
- 상황별 반응 팔레트 (고정 4개로)
- 빈도 설정 UI (컬럼만 두고 전부 주 3회)
- 밤 10시 크론 (오늘은 수동 버튼)
- 회수권, 2주 쉬기, 서랍 달력, 알림, 연령 확인, 신고·차단

### 왜 안전 필터를 기본 잠김으로 대체하나
LLM 필터가 없으면 실명·회사명이 그대로 나갈 수 있다. 그래서 **오늘은 opt-in으로 뒤집는다** — 사용자가 자물쇠를 직접 푼 문단만 후보가 된다. 나중에 필터를 붙이면 `default false`로 되돌리고 opt-out으로 전환한다.

---

## 스택

- **Next.js 15 (App Router)** + TypeScript, Server Actions 위주 (API 라우트 최소화)
- **Supabase** — Postgres + Auth(매직링크). `@supabase/ssr` 사용
- **Vercel** 배포, GitHub 형상관리
- **스타일**: Tailwind 안 씀. `globals.css`에 직접 작성 (설치 마찰 제거)
- 폰트: Gowun Batang(일기 본문) + system sans(UI). Google Fonts

### 팔레트
```
--ink #141A18   --paper #E9ECE8   --surface #F8FAF7
--muted #5C6863 --line #CBD3CC
--accent #0D6B60 (딥 틸)   --accent-wash #DCEAE6
--signal #8E3A6B (플럼, 반응/치환어)   --signal-wash #F3E1EC
```
다크모드는 오늘 안 함.

---

## 이미 있는 파일

- `schema.sql` — Supabase SQL Editor에 통째로 붙여넣기. RLS 정책과 `feed` 뷰 포함
- `src/lib/buoy.ts` — 문단 분리, 욕 사전(150개) 치환, 점선 밑줄 렌더 조각, 추첨. **전부 순수 함수, 의존성 없음**
- `package.json` / `tsconfig.json` / `next.config.mjs` / `.env.example` / `.gitignore`

---

## 만들어야 할 파일

```
src/
  middleware.ts                    Supabase 세션 갱신
  lib/
    supabase/client.ts             브라우저용
    supabase/server.ts             서버 컴포넌트/액션용 (cookies는 await)
    supabase/admin.ts              service_role. 서버에서만
    nicknames.ts                   "밤의 우체통" 같은 조합 생성
    prompts.ts                     매일 바뀌는 질문 (날짜로 인덱싱)
    draw.ts                        추첨 파이프라인 (buoy.ts 함수 사용)
  app/
    layout.tsx  globals.css
    page.tsx                       오늘 — 쓰기 화면 (홈이 곧 작성 화면)
    actions.ts                     saveEntry / toggleLock / toggleReaction / drawForMe
    login/page.tsx                 이메일 입력 → signInWithOtp
    auth/callback/route.ts         token_hash와 code 둘 다 처리
    onboarding/page.tsx            닉네임 배정 + 공개 고지 동의
    feed/page.tsx                  표류
    mine/page.tsx                  내 발췌
    api/cron/draw/route.ts         Bearer CRON_SECRET 검사 후 전체 추첨
```

---

## 절대 규칙

1. **`paragraphs.text`(원문)를 번역본으로 덮어쓰지 말 것.** 번역본은 `drafts.translated_text`에 따로. 이걸 뭉개면 서랍의 원문/번역본 비교 화면을 영원히 못 만든다.
2. **발췌는 `Math.random()`으로 뽑을 것.** 사람이 고르면 검증이 무의미해진다.
3. **RLS를 끄지 말 것.** 안 되면 정책을 고친다. 남의 일기가 새는 걸 막는 마지막 방어선.
4. **`SUPABASE_SERVICE_ROLE_KEY`가 클라이언트 번들에 들어가지 않게.** `NEXT_PUBLIC_` 접두사 금지, 서버 라우트/액션에서만.
5. **회수는 삭제가 아니라 상태.** `retracted_at` 타임스탬프.

---

## 오늘만 쓰는 꼼수 (`INSTANT_MODE=true`)

- `eligible_from` 조건을 건너뛴다 → 방금 쓴 글도 바로 뽑힌다
- `publish_at = now()` → 밤 10시 안 기다리고 즉시 공개
- 홈에 추첨을 수동 실행하는 임시 버튼

**배포 확인 끝나면 되돌릴 것:**
- [ ] `INSTANT_MODE=false`
- [ ] 수동 추첨 버튼 제거 → Vercel Cron (`vercel.json`에 낮 준비 / 밤 10시 공개 2개)
- [ ] LLM 안전 필터 추가 → `locked default false`로 전환

---

## 카피 톤

- 반말. 친구 말투. 상담사 말투 절대 금지
- "힐링", "괜찮아요", "당신은 소중해요" 금지
- 핵심 동사는 **"들키다"** — "얼마나 들킬래?", "들켰다", "가끔만 들켜도 돼"
- 마이크 버튼 아래: "누르고 그냥 말해. 욕해도 돼."
- 온보딩 첫 화면에 **화면 한복판**으로: "당신이 쓴 글의 일부가 예고 없이 공개됩니다. 자물쇠를 푼 문단만 후보가 됩니다."

---

## 빌드 순서 (이 순서를 지킬 것)

1. **빈 페이지부터 Vercel 배포 성공시키기** (40분) — 다 만들고 밤에 배포하면 환경변수/빌드 에러로 막힌다
2. Supabase 프로젝트 + `schema.sql` 실행 + 매직링크 로그인 (30분)
3. 쓰기 + 문단 분리 + 자물쇠 토글 (1시간)
4. 추첨 + 수동 버튼 (40분)
5. 피드 + 점선 밑줄 렌더링 (1시간) ← **점선 밑줄은 빼지 말 것. 시그니처.**
6. 반응 버튼 (40분)
